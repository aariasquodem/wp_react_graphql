import React from "react";
import Nav from '../Nav';

const Header = () => {
  return <header>
          <h1 className="page-title">React WPHeadles</h1>
          <Nav/>
        </header>;
};

export default Header;
